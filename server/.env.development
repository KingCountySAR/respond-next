# Dev-only server settings, loaded by `tsx watch --env-file=.env.development`.
# Non-secret values only (this file is committed). Secrets live in the repo-root
# .env / .env.local, which server/src/env.ts loads at startup.
PORT=5173
